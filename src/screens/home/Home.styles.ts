import {StyleSheet} from 'react-native';
import {Spacing16} from '@constants';

export const styles = StyleSheet.create({
  summaryContainer: {
    flexDirection: 'row',
    marginBottom: Spacing16,
  },
});
